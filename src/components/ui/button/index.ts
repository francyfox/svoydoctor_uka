import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as UButton,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
