name: Keep Directus awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Directus admin
        run: |
          curl -fsS -o /dev/null -w "status: %{http_code}\n" https://directus-12-2.onrender.com
